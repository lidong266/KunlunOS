{
  "name": "pi-kunlun",
  "version": "0.1.0",
  "private": true,
  "description": "Pi-昆仑 V2 — 三进制认知基础设施",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
